    function draw(ctx, color) {

      // mouth/10
      ctx.save();

      // mouth/10/stroke
      ctx.save();
      ctx.beginPath();
      ctx.moveTo(314.3, 372.8);
      ctx.lineTo(200.0, 372.8);
      ctx.lineWidth = 15.0;
      ctx.strokeStyle = "rgb(35, 30, 32)";
      ctx.lineCap = "round";
      ctx.lineJoin = "round";
      ctx.stroke();
      ctx.restore();
      ctx.restore();
    }
